# メタグモ タスクリスト

# 現在の作業方針（2026-06-03）
- [/] R1 MVPコア機能整備を継続する。
  - [ ] 直近は R1-2 APIルート実装と R1-3 タグリスト/視界トグル/ソート機能を優先する。
  - [ ] ファクトは中立・出典必須、タグは主観ラベルという分離を崩さない。
  - [ ] `flat` / `registered_only` / `leverage` の3視界を MVP の中核体験として扱う。
  - [ ] Leverage 計算式は `metagmo_spec.md` の安全版（-3〜+3を0〜2倍に変換、負の反転効果なし）を厳守する。
- [ ] UI はできる限りシンプルで馴染みやすい方向にする。
  - [ ] SNSらしい派手さより、Wiki / 掲示板 / 検索結果に近い、初見で迷わない情報配置を優先する。
  - [ ] 視界トグル、タグ、投票、出典、モデレーション状態が過剰な装飾に埋もれないようにする。
  - [ ] Leverage など新しい概念は専門用語を前面に出しすぎず、自然に切り替えて理解できるUIにする。
- [ ] R1-4 以降は、R1-2/R1-3 のデータ取得・スコアリング境界が固まってから進める。
- [ ] 実装時は、匿名CAPTCHA/30秒クールダウン、通報→一時非表示、モデレーター確認フローを後付けにしすぎない。

# 公開準備
- [x] Git公開準備: Supabase/Vercel の環境変数、credential、DB dump、ローカルデータを除外する `.gitignore` を追加

# チェックリスト
1. [x] R1-1 DBスキーマ定義
2. [ ] R1-2 APIルート実装（認証なし）
3. [ ] R1-3 ワードクラウド表示実装
4. [ ] R1-4 視界ボタン実装（直近一週間・合意・論争）
5. [ ] R1-5 タグ検索・ソート機能実装

## 次のアクション
- [ ] R1-2 認証不要のAPIルートを実装する（entities / facts / tags / votes）
- [ ] R1-3 ワードクラウドコンポーネントを実装する（単語サイズ = ネットスコア）

---

## R1-1 DBスキーマ定義
- [x] `metagmo_spec.md` のデータモデル（MVP最小版）に基づき、Supabase (PostgreSQL) 用のSQLスキーマを定義する。
- [x] `Entity`, `Fact`, `Tag`, `Vote`, `History` テーブルの設計を行う（`Follow` は発展形用として定義済み）。
- [x] 各テーブルのカラム、データ型、主キー、外部キー、インデックスを決定する。

## R1-2 APIルート実装（認証なし）
- [ ] `metagmo_spec.md` セクション2.1に基づき、以下のAPIエンドポイントをNext.jsのAPIルートとして実装する。
  - `GET /api/entities` — ページ一覧
  - `GET /api/entities/[id]` — ページ詳細
  - `POST /api/entities` — ページ作成（匿名）
  - `GET /api/entities/[id]/facts` — ファクト一覧
  - `POST /api/entities/[id]/facts` — ファクト作成（匿名・出典URL必須）
  - `GET /api/entities/[id]/tags` — タグ一覧（視界パラメータ対応）
  - `POST /api/entities/[id]/tags` — タグ投稿（匿名）
  - `POST /api/tags/[id]/vote` — up/down投票（匿名）
- [ ] 各エンドポイントの入力バリデーション・ビジネスロジック・DB操作を実装する。
- [ ] 認証は不要（MVP）。Supabase の匿名アクセスポリシーを設定する。

## R1-3 ワードクラウド表示実装
- [ ] ページに紐づくタグをワードクラウドで表示するコンポーネントを実装する。
- [ ] 単語サイズ = `up - down`（ネットスコア）で決定する。
- [ ] デフォルト表示: 各タグに `👍up数 👎down数 合計` を表示する（UIが煩雑な場合は合計のみ）。
- [ ] 数字のホバー表示 vs 常時表示をUI実装時に確認して決定する。

## R1-4 視界ボタン実装（直近一週間・合意・論争）
- [ ] 3つのトグルボタンを実装する（未押し→1回目→2回目→未押し の3状態循環）。
- [ ] **直近一週間**: 1回目 = 7日以内の投票のみ集計 / 2回目 = 7日より前の投票のみ集計。
- [ ] **合意**: 1回目 = `up/(up+down) ≥ 0.7` かつ `up+down ≥ 3` / 2回目 = `down/(up+down) ≥ 0.7` かつ `up+down ≥ 3`。
- [ ] **論争**: 1回目 = `0.3 ≤ up/(up+down) ≤ 0.7` かつ `up+down ≥ 3` / 2回目 = 決着済み（合意または拒絶に振り切れたタグ）。
- [ ] ボタン状態をUIで色分けする（未押し: グレー / 1回目: 青 / 2回目: 橙）。
- [ ] 複数ボタンの組み合わせ適用ロジックを実装する（ANDフィルター）。

## R1-5 匿名ユーザー向けCAPTCHA/クールダウン実装
- [ ] 匿名ユーザーがファクト作成、タグ投稿、投票を行う際にCAPTCHA認証を導入する。
- [ ] 匿名ユーザーの投稿・投票に対して30秒のクールダウン期間を設ける。

## R1-6 モデレーション機能実装
- [ ] タグやファクトに対する通報機能を実装する。
- [ ] 通報が一定のしきい値に達した場合、対象コンテンツを自動的に一時非表示にする。
- [ ] モデレーターが通報されたコンテンツを確認し、確定/差戻し、または半保護設定を行える簡易モデレーターページを実装する。
